{
  "name": "multi-tenant-react-app",
  "version": "1.0.0",
  "description": "A milti tenant React app with server",
  "main": "server.js",
  "scripts": {
    "server": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shubham jayswal",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
